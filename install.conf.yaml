- defaults:
    link:
      create: true
      relink: true

- clean: ['~', '~/.config']

- link:
    ~/.dotfiles: ''
    ~/.tmux.conf:
    ~/.vim:
    ~/.vimrc:
    ~/.gitconfig:
    ~/.gitignore_global:
    ~/.condarc:
    ~/.scripts:
    ~/.shell:
    ~/.bash:
    ~/.bashrc:
    ~/.bash_profile:
    ~/.zsh:
    ~/.zshrc:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
  - [~/.zsh/oh-my-zsh.sh, Installing oh-my-zsh]
